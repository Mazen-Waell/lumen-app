export default function Footer() {
  return (
    <footer className="app-footer">
      <span><strong style={{color:'var(--text)'}}>lumen</strong> — Turn Chaos into a Brief</span>
      <span>Powered by <strong style={{color:'var(--text)'}}>SynapX</strong></span>
    </footer>
  )
}
